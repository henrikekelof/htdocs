---
import Master from './master/Master.astro';
const { frontmatter, indexing, title } = Astro.props;
---

<Master
   frontmatter={frontmatter}
   indexing={indexing}
   title={title}
   heading={false}
   bodyClass="wide"
>
   <slot />
</Master>
