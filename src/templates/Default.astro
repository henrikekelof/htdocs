---
import Master from './master/Master.astro';
const { frontmatter, heading, indexing, title } = Astro.props;
---

<Master
   frontmatter={frontmatter}
   heading={heading}
   indexing={indexing}
   title={title}
>
   <slot />
</Master>
